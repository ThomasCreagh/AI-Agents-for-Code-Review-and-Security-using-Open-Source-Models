# SwEng25_Group_23_IBM

### Docker

Run and Build:

```console
$ sudo docker compose up -d --build
```

Run without Build:

```console
$ sudo docker compose up -d
```

Stop:

```console
$ sudo docker compose down
```

test
